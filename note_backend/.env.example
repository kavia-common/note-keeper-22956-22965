# Copy this file to .env and set values for your environment
# SECRET_KEY is required in production. A random 32+ char string is recommended.
SECRET_KEY=replace-with-a-secure-random-string

# Optional: override sqlite path or use another DB (e.g., postgres)
# DATABASE_URL=sqlite:///absolute/path/to/note.db
